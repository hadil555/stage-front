import { HighlightEmailPipe } from './highlight-email.pipe';

describe('HighlightEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
