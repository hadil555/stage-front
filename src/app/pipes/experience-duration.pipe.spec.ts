import { ExperienceDurationPipe } from './experience-duration.pipe';

describe('ExperienceDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new ExperienceDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
